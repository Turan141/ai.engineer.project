import type { IAgentPlan } from "../../shared/interfaces/planner.interface.js"

export class PlannerService {
	async createPlan(request: string): Promise<IAgentPlan> {
		// Placeholder implementation - replace with actual planning logic
		throw new Error("PlannerService.createPlan is not implemented yet")
	}
}
